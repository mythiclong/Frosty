# 2026-09-23 Frosty 工作日志

## v1.3.0-beta.2.1+26.2 完成（构建 + 推送）

分支 `workbuddy/v1-3-0-beta2-1-26-2`（基于 origin/master = MC 26.2），commit 3e3c7f5，已推送 mythiclong/Frosty。
产物：`build/libs/Frosty-1.3.0-beta.2.1+26.2.jar`（2.4MB），构建一次通过（修复 8 个编译错误后）。

### 26.2 关键 API 备忘（本次踩坑记录，复用价值高）
- `WaterAnimal` 已删除 → 水生物用 `world.entity.animal.AgeableWaterCreature`
- `Slime` → `world.entity.monster.cubemob.Slime`
- `Villager` → `world.entity.npc.villager.Villager`（或 `AbstractVillager` 覆盖 WanderingTrader）
- `Scoreboard.listPlayerScores(Objective)` 返回 `Collection<PlayerScoreEntry>`（record：`owner()` String / `value()` int / `ownerName()` Component），不再是 Map
- `KeyMapping.key` 是 protected，无 `getKey()`；公开 API 用 `matches(InputConstants.Key)` 或 `getDefaultKey()`
- `mc.screen` → `mc.gui.screen()`
- 查 jar 类路径：`jar tf minecraft-merged-deobf-26.2.jar | grep ...`，javap 在 Adoptium jdk-25 bin

### 本次完成内容
- 全部 12 项 bug 修复（Sodium Xray 死链、SettingUpdateEvent import、IKeyMapping 自递归、3 处 NPE、3 处逻辑错误、3 处隐患）在 26.2 基底落地
- Nametags 参考 OpenMyau 1.8.9 全量重构（距离/血量模式、血量渐变色、自动缩放公式、实体过滤、NickHider/AntiBot 集成），用 26.2 渲染 API（BufferSource、Font.prepareText + GlyphVisitor、SEE_THROUGH）
- 删除 Vec3Mixin/IVec3d/SubmitNodeCollectionMixin；EntityRendererMixin 改为仅接管目标时抑制原版名牌
- 26.1.2 线工作已作为 archive 提交（c1d24a2），从此版本仅支持 26.2

### 待办
- 等用户实测反馈（Xray + Sodium、Nametags 渲染、AutoClicker Hold、ConnectionMixin bundle 过滤）
- 沙箱内 git push 会被 SIGTERM，需 dangerouslyDisableSandbox + GIT_TERMINAL_PROMPT=0
